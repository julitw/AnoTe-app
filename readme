An application that enables data annotation using large language models and generates explainability for the annotations.

1. Clone the Repository
Clone the repository to your computer:

git clone --recurse-submodules https://github.com/julitw/Anote-APP.git
cd Anote-APP


2. Create a .env File
Create a .env file in the project folder with the following content:

POSTGRES_USER=your_user
POSTGRES_PASSWORD=your_password
POSTGRES_DB=your_database
POSTGRES_HOST=database 
POSTGRES_PORT=5432
CLARIN_API_KEY=your_api_key


3. Run the Application with Docker
To start the application, run:

docker-compose up --build



